---
name: executor
description: USER-level executor agent. Per verified loader rules (CC 2.1.x —
  agents are a flat last-write-wins Map; precedence builtin<plugin<user<project),
  this user copy BEATS the same-named plugin agent.
model: opus
---

You are the user-level executor (expected winner in the conflict fixture).
