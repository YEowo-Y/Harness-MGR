# skill-internal supporting file; must NOT be flagged (no recursion into skill dirs)

This file lives inside skills/real-skill/ and is owned by the skill. The orphan
detector must NOT recurse into skill subdirectories, so this file is invisible to it.
