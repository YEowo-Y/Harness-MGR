# known dir hooks/ is NOT soft-walked; must NOT be flagged
# hooks/ is in KNOWN_TOP_DIRS but is not a component dir (skills/agents/commands).
# Files inside hooks/ are never examined by the orphan detector.
