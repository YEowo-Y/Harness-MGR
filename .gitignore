node_modules/
coverage/
*.tmp
*.mgr-new
.DS_Store
.mgr-state/
.env
.env*.local
*.log
# ...but DO track audit-log fixtures read by test/audit.test.mjs (P2.U10).
!test/fixtures/**/*.log
# ...and the synthetic .env secret fixtures for snapshot-secrets tests (P3.U4).
!test/fixtures/secrets/.env
!test/fixtures/secrets/.env.local
.omc/

# Go TUI build artifacts (tui/)
*.exe
*.exe~

# Local dev artifacts — kept on disk for AI sessions, not repo content.
# (build journal, generated agent context, per-machine IDE/tool config)
WORKING-CONTEXT.md
AGENTS.md
.claude/
.codex/

# Internal design / handoff / review notes — kept locally, not shipped publicly.
docs/internal/

# Personal, per-machine Claude Code session prefs (not team-shared).
CLAUDE.local.md
