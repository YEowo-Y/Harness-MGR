# claude-mgr — project context for Claude Code sessions

A **read-mostly governance CLI** for the user's large Claude Code harness (inventory / conflicts / `config show-effective` / doctor / snapshot-rollback / remove-update). Zero-runtime-dependency Node ESM, Windows-hardened, **dry-run-by-default**, lives OUTSIDE Claude Code's loader. The user is a non-coder ("vibe coding") — reply in Chinese.

## Authoritative references (read these before building)
- **Full plan (1624 lines):** `C:\Users\alice\.claude\plans\claude-mgr-v5.md` — the single source of truth (decided-items table + verified loader rules + 67-unit schedule + test catalog).
- **Session memory / decisions / learnings:** `C:\Users\alice\.claude\projects\C--Users-alice\memory\MEMORY.md`.

## Build cursor (as of 2026-05-25)
- ✅ **P1.U1** scaffold · **P1.U2–U5** core lib (Diagnostic/Source/Plan/Result + DiagnosticBag; `paths.mjs`+`assertWritable` [fail-closed, symlink-escape denied]; `retry`; `safe-spawn` [mandatory argv schema, deny-by-default]; async `reexport` shim) · **P1.U6** fixtures (incl. redacted `real-snapshot/`) · **P1.U7** discovery/components (split into `frontmatter.mjs` [pure, null-proto, never-throws YAML-frontmatter parser] + `components.mjs` [FLAT skill/agent/command walk → `ComponentRecord[]`+`Diagnostic[]`]; reviewer APPROVE after HIGH-1 null-proto hardening + HIGH-2 rejected with evidence; namespaced-command recursion deferred to a unit with a nested fixture) · **P1.U8** discovery/plugins+marketplaces (`read-json.mjs` shared never-throws JSON reader; `plugins.mjs` installed_plugins.json→`PluginRecord[]` [schema-version-first; `cachePresent` is a FACT, not a judgment]; `marketplaces.mjs` known_marketplaces.json→`MarketplaceRecord[]` [root-relative `onDisk`, not trusting machine-specific `installLocation`]; new PII-redacted `plugins-groundtruth/` fixture = committed 13-installed / 11-missing-cache / 2-onDisk oracle; reviewer APPROVE, 0 Blocker/High) · **P1.U9** discovery/settings+mcp (`settings.mjs` [discoverSettings→statusLine capture, discoverTopLevelDirs→19-known-dir classification, KNOWN_TOP_DIRS exported]; `mcp.mjs` [discoverMcp project+user scope, transport stdio/http/unknown, SECRET-SAFE envKeys=key-names-only]; `settings-mcp/` fixture incl. MUSTNOTLEAK sentinel; reviewer APPROVE after LOW-1/LOW-2/MEDIUM-1/MEDIUM-2 adopted) · **P1.U10** discovery/scan (`scan.mjs` [scan({targetClaudeDir,appFile?,kinds?})→ScanResult; ALL_KINDS=['components','plugins','settings','mcp']; kinds filter with empty/unknown fallback to all; MEDIUM-1/MEDIUM-2 → JSDoc contract notes]; reviewer APPROVE 0 Blocker/High) · **P1.U11** discovery/orphan-detector (`orphan-detector.mjs` [`detectOrphans(rootDir,opts?)`→`{hard,soft,diagnostics}`; LOCATION-based per user decision: hard=top-level entry not in KNOWN_TOP_DIRS/KNOWN_TOP_FILES/ownTopDirs; soft=loose file in skills/ OR non-`.md` file in agents/commands/; subdirs NOT walked (skill-internals are skill-owned + agents/commands subdirs reserved for deferred namespacing); orphans are FACTS (doctor #12 judges); pure/sync — `ownTopDirs` default `['.mgr-state','.mgr']` injected at U15 instead of importing async paths.mjs, with a drift-guard test reconciling `DEFAULT_OWN_TOP_DIRS[0]`↔`MGR_STATE_DIRNAME`]; new `orphan/` fixture oracle [hard=2, soft=3; `.mgr-state` is gitignored→tested via temp dir, `.mgr` lives in fixture; fixture files must avoid `.js/.mjs/.cjs` — `node --test` auto-collects `**/test/**/*.js`]; reviewer APPROVE after HIGH drift-guard + MEDIUM all-known-files coverage + LOW conservative-list doc adopted). · **P1.U12** analysis/conflicts+orphans — VERTICAL SLICE, skills-only (`conflicts.mjs` [`analyzeConflicts(components)`→`{conflicts,diagnostics}`; pure/never-throws; clusters skills by VERIFIED resolution key — plugin components namespaced `pluginName:skill`, user/project FLAT ⇒ user-vs-plugin is NOT a collision (no false positive); the handled skill collision is plugin-vs-plugin (same plugin name from ≥2 marketplaces); each cluster emits `likelyWinner`+`possibleWinners[]` with `confidence:'likely'` (Phase-1 can't assert a verified `winner` — CC version unknown + inter-plugin order unverified) + a flat `skill-shadowing` warn `Diagnostic`; `isEligibleSkill` excludes plugin records lacking a non-empty `source.plugin` (else flat-collide); agents/commands = TODO(U13); local `SKILL_RANK` = TODO(U14) debt to unify into `load-order.mjs` per the single-source-of-truth invariant]; `orphans.mjs` [`analyzeOrphans(orphanResult)`→`{orphans:[...hard,...soft],summary:{hard,soft,total},diagnostics}`; THIN flatten over U11 `detectOrphans`, NO re-walk + NO judgment; tolerant coercion; passes discovery diagnostics through as the authoritative set (double-count caveat like scan.mjs)]; reviewer APPROVE — conflicts after HIGH-1 plugin-missing-`plugin`-field false-positive fix + MEDIUM mixed-array/degenerate-plugin tests; orphans after MEDIUM unit-label + LOW undefined-boundary; NOTE the `conflict/` fixture's real cluster is the `executor` AGENT collision → correctly deferred to U13, so the skill slice is tested via synthetic plugin-vs-plugin records + a `conflict/` scan smoke). · **P1.U13** analysis/conflicts(all-tiers)+settings-merge+docs (`conflicts.mjs` extended skills-only→**skill+agent+command** via one `KIND_RULES` table [per-kind `namespacePlugins`+`ranks`, "lower wins"]: skills/commands stay plugin-namespaced first-match (user-vs-plugin still NOT a collision); **agents FLAT for all tiers + last-write-wins** ⇒ user-vs-plugin agents DO collide, USER wins (ranks invert last-write-wins so user 3 < plugin 4); group by `(kind,resolutionKey)`, sort `(kind,key)`, per-kind `${kind}-shadowing` code; `isEligibleComponent` keeps the namespaced-kind non-empty-`plugin` guard + now excludes non-string names; bundled-shadows-user still needs an unmodeled `'bundled'` tier; reviewer APPROVE, MEDIUM 3-kind-sort + non-string-name guard adopted) · `settings-merge.mjs` [`mergeSettings(layers)` lowest→highest → `{effective,keys,diagnostics}`; per-key: permissions.allow/ask/deny array-union, hooks per-event concat (hookDedupKey→Phase-2), env/enabledPlugins object-merge later-wins, known scalars highest-wins; undocumented key → `mergeConfidence:'unknown'`+per-layer raw, NO fabricated effective; **prototype-poisoning hardened** via `isSafeKey` skipping `__proto__`/`constructor`/`prototype` at every key-iteration site; reviewer REQUEST-CHANGES→APPROVE after HIGH scalar-highest empty-`present` guard + MEDIUM __proto__ hardening/test + immutability/single-layer tests] · `docs/effective-config-rules.md` (layer precedence + per-key table + unknown policy + verified-source citation). · **P1.U14** analysis/load-order + output adapters + conflicts-refactor (`load-order.mjs` [now the SINGLE SOURCE OF TRUTH for precedence: `KIND_RULES` (per-kind `namespacePlugins`+`ranks`+`winsBy`), `resolutionKey`, `isLoadableComponent`, `rankComponents` (rank ASC then **ES6 insertion-order tiebreak** honoring `winsBy` — 'first' for skills/commands, 'last' for agents), `loaderConfidence(ccVersion)` = **2.1.x version guard** via `/^2\.1(?:\.\d+)?$/` (verified→`winner`; absent→`likely`+info; off-line→`likely`+warn `loader-rules-unverified-version`); pure/never-throws — malformed members rank last; reviewer REQUEST-CHANGES→APPROVE after HIGH rankComponents/resolutionKey never-throws guards + MEDIUM regex tighten rejecting `2.1.x`/`2.1.`/`2.10`] · `output/json.mjs` [`version:1` envelope (version written LAST so a payload `version` can't clobber it); `stableStringify` deterministic recursive key-sort, arrays preserved, prototype-poisoning keys dropped, cycle/BigInt→error envelope; reviewer REQUEST-CHANGES→APPROVE after HIGH spread-order + MEDIUM proto guard] · `output/table.mjs` [aligned monospaced table, plain-by-default, `colorize` NO_COLOR-aware; CJK width = documented Phase-1 limit] · **conflicts.mjs REFACTORED** to import precedence from `load-order.mjs` — deletes ~90 LOC local `KIND_RULES`/ranking, satisfying the single-source-of-truth invariant; behavior-preserving (reviewer APPROVE); also replaced a NUL-byte groupKey separator with `\n` for grep-safety). · **P1.U15** cli — the read-CLI capstone (`cli.mjs` [`run(argv)`→`{code,stdout}`; pure/unit-testable — no `process.exit`/stdout; whole body try/caught → JSON error envelope, NEVER a bare stack; hand-rolled zero-dep argv parse incl. two-word `config show-effective`→`config:show-effective`; null-proto args; frozen `COMMANDS` `hasOwnProperty` dispatch; `--format` json(`version:1`)/table/quiet; exit codes 2=usage/throw, 1=error-diag, 0=clean] · `cli/commands.mjs` [6 pure handlers `(ctx={configDir,args})→{result,diagnostics}` wiring scan/conflicts/orphans/settings-merge/load-order; `readSettingsLayers` user<local read-once; conflicts appends `loaderConfidence(undefined)` Phase-1 info] · `cli/resolve-config.mjs` [**M2 RESOLVED**: explicit `--config-dir` wins; else dynamic-import paths.mjs — a missing `~/.claude/hooks/lib` (reexport top-level-await reject) degrades to a `missing-hooks-lib` warn + `CLAUDE_CONFIG_DIR`/`~/.claude` fallback so reads still work; injectable `loadPaths` test seam] · `cli/render.mjs` [per-command table + quiet, fully defensive]; reviewer APPROVE both sub-units — commands after MEDIUM homedir-fallback test + `--type marketplace`; shell after MEDIUM null-proto args + null-argv test). · **P1.U16 (PHASE-1 GATE)** selftest gates (`selftest/projection.mjs` [comment/string/regex code-projection engine] + `lint.mjs` [SLOC linter: **module ≤200 / fn ≤80 / param ≤5**, `// mgr-lint-ignore: <reason>` pragma escape — fn limit **80 per plan U16 line** (owner-decided; the tighter ≤30 is deferred to the release-gate phase); `regexStarts` keyword-aware after review] + `invariants.mjs` [static load-order single-source-of-truth guard: conflicts.mjs MUST import resolutionKey/isLoadableComponent/rankComponents from load-order.mjs + MUST NOT define KIND_RULES (const/let/var) + load-order MUST `export const KIND_RULES`] + `boundary.mjs` [static forbidden-import scan incl. dynamic `import()` + runtime write-allowlist probe over INJECTED assertWritable/roots; M2-degrades to static-only + `boundary-runtime-skipped`]) wired into `selftest --lint/--invariants/--boundary/--all` (selftestCommand now async; `cli.mjs` await-dispatch + executable `main` guard so `claude-mgr.ps1` produces output) · `detectOrphans` refactored into helpers (`resolveEntryType`/`classifyTopLevel`/`collectSoftOrphans`/compareHard/Soft) to satisfy the gate — behavior-preserving (26 tests, reviewer APPROVE) · `claude-mgr.ps1` thin entry + `scripts/install-dev.ps1`/`uninstall-dev.ps1` (dogfood symlink via `cmd /c mklink /D`, **reparse-point-guarded** removal — never `Remove-Item -Recurse`) · all reviewers APPROVE (selftest trio + PS scripts + wiring + orphan refactor); full-src lint CLEAN (31 files); real-harness `inventory` = **240 skills / 19 agents / 79 commands / 0 diagnostics**, `selftest --all` exit 0. All committed, **391 tests pass / 0 fail** on Node v24.14.0. Tagged **`phase-1-stable`** — PHASE 1 COMPLETE. ▷ **PHASE 2 (Doctor + Drift + JSONC):** **P2.U1** `src/lib/jsonc-parser.mjs` happy-path JSONC tokenizer [`parseJsonc(text)`→`{value,errors,duplicateKeys}`, NEVER-throws; tolerates `//` + single-level `/* */` comments + trailing commas; duplicate keys → LAST-wins value + 1-based `line:column`; `Object.create(null)` values (proto-safe); syntax error → `errors[]` + `value:undefined`; non-string input → error, not throw; reviewer APPROVE 0 Blocker/High — MEDIUM colon-error column off-by-one FIXED (capture pos before `advance`), 2 LOWs intentional; BOM / nested comments / `\uXXXX` surrogate pairs / exhaustive `line:column` DEFERRED to P2.U2]. **405 tests / 0 fail**. · **P2.U2** edge-case fixtures + tokenizer hardening [BOM strip in `parseJsonc` (single leading U+FEFF; interior BOM preserved); nested block comments PINNED to JSONC/JSON5 "first `*/` wins" — no depth-nesting — via test; surrogate-pair + lone-surrogate coverage; exact duplicate-key `line:column` across LF **and** CRLF; new `test/fixtures/jsonc-edge-cases/` carries a real EF BB BF BOM + genuine CRLF, byte-protected by a local `.gitattributes` `-text` so the repo-root `* text=auto eol=lf` can't normalize them; reviewer (opus) APPROVE 0 Blocker/High/Medium + 2 trivial LOW; 17 edge tests; selftest `--all` exit 0; commits `ff617e9`/`6cb64ca`/`b5d73a9` pushed]. **426 tests / 0 fail**. · **P2.U3** wire jsonc-parser into settings discovery [`read-json.mjs` gains `readJsoncFile` — the tolerant sibling of strict `readJsonFile`: same never-throws I/O + ENOENT handling, parses via `parseJsonc`, returns `{value,error,missing,duplicateKeys}`; first parse error → one-line `invalid JSONC: <msg> (line L, column C)`; `value` normalized to `null` on error (JSDoc-noted vs parseJsonc's `undefined` — check `error`/`missing`, not value); the STRICT reader stays for plugins/marketplaces/mcp] · `settings.mjs::discoverSettings` switched to `readJsoncFile` + emits one `warn`/`settings-duplicate-key` per repeated key with 1-based line:column in the message (FACTS for doctor #6 `settings-json-valid` P2.U4 to judge) · new `test/fixtures/settings-dupkey/` oracle (dup `"model"` at line 3 col 3, last-wins `opus`) · `settings.test.mjs`: old "trailing-comma→unreadable" test REPLACED with JSONC-tolerance (`broken/` now `present:true`, no error) + genuinely-malformed→`settings-unreadable` + comments/trailing-comma tolerance + dupkey line:column · SCOPE DECISION: `readSettingsLayers` (cli/commands.mjs, powers `config show-effective`) deliberately LEFT on strict `JSON.parse` with a `TODO(P2)` note — reviewer (separate opus pass) confirmed this is an acceptable plan-scoped boundary, NOT a contradiction (both paths fail gracefully) · real-harness `inventory` = 0 diagnostics (real settings.json parses clean under JSONC); reviewer APPROVE 0 Blocker/0 High, MEDIUM value-null JSDoc + scope-TODO adopted. **429 tests / 0 fail**. · **P2.U4** analysis/doctor scaffold + first 3 passive checks (`src/analysis/doctor/index.mjs` [`runDoctor(input,opts)`→`{probeLevel,checks[],diagnostics[]}`; PURE/sync/never-throws JUDGMENT layer over already-gathered facts — does NO I/O, so passive = statically side-effect-free + sails through the boundary gate (imports only DiagnosticBag); passive/active dispatch filters per-check `probeLevel` — active runs ONLY when `opts.activeProbes` (none registered till U7) + emits one `doctor-active-probes` info notice; `opts.checks` injectable test-seam; per-check try/catch → `doctor-check-threw` containment so one bad check can't abort the run; frozen `CHECKS` registry; unrecognised probeLevel fails SAFE to passive] · 3 passive checks: **#6 `settings-json-valid`** [escalates the discovery FACTS `settings-duplicate-key`/`settings-unreadable`/`settings-malformed` to ERROR preserving 1-based line:column; per-cause `fix` message; filters by code so feeding the whole scan set is safe], **#7 `plugin-enabled-not-installed`** [enabled≠installed: a key `true` in the merged `enabledPlugins` map but absent from the installed `PluginRecord.key` set → error; `isSafeKey` `__proto__` guard; key-sorted output], **#11 `duplicate-component-shadowing`** [conflict cluster >1 member → warn, reusing the cluster's reason/fix] · reviewer (code-reviewer) APPROVE 0 Blocker/0 High; 2 MEDIUM (proto-guard on `enabledPlugins` iteration + #6 second-fact path-propagation assertion) + 2 LOW (per-cause fix string + explicit `__proto__` test) ALL adopted · tests off REAL fixtures (`settings-dupkey` #6, `plugins-groundtruth` #7) + synthetic clusters (#11 — Phase-1 `scan()` still doesn't walk `plugins/cache/` component dirs, so a real scan can't produce a cluster; the doctor is a pure consumer so this is fine) · `doctor` CLI wiring DEFERRED to P2.U11 per plan (this unit = module + tests only). **455 tests / 0 fail**. · **P2.U5a** (split from plan U5 — the 3 PURE plugin checks; the 4 I/O checks #1/#2/#3/#5 deferred to U5b) doctor **#8 `plugin-installed-not-enabled`** [installed but not `true` in the settings `enabledPlugins` map → info], **#9 `plugin-marketplace-unknown`** [installed plugin's `marketplace` not in the known set (new `marketplaces` field on DoctorInput = scan.marketplaces) → info; SKIP-when-empty: no baseline ⇒ would flood, and a missing known_marketplaces.json is its own discovery diag], **#10 `plugin-cache-missing`** [settings-enabled install + `cachePresent===false` → warn; cachePresent is a discovered FACT] · shared `pluginList` + `enabledMap` helpers; CHECKS reordered numeric [6,7,8,9,10,11]; doctor STAYS pure (no I/O) · **ENABLE MODEL** (corrected by the dogfood — see STABILITY-LOG.md + [[claude-code-plugin-enable-model]]): #7/#8/#10 ALL key off the settings `enabledPlugins` map (the AUTHORITATIVE signal); installed_plugins.json's per-entry `enabled` is NOT consulted (it is `false` even for active plugins). #7 = in-map-true but NOT installed; #8 = installed but NOT in-map-true — disjoint, no double-count · **DOGFOOD (Phase-1 gate, real ~/.claude):** wired CLI `inventory`(240 skills/19 agents/79 cmds, 14 plugins, 6 mcp)/`conflicts`(0)/`orphans`/`selftest --all` all clean; exercising the not-yet-wired doctor over real facts CAUGHT A FALSE POSITIVE — the original #8/#10 read the install record's `enabled` (=`false` for ALL 14 actively-used plugins) → #8 fired 14× + #10 latent false-negative; the settings-map fix makes the real-harness doctor report **0 diagnostics across all 6 checks** (logged in STABILITY-LOG.md) · reviewer (resumed code-reviewer, 3 passes) APPROVE 0 Blocker/0 High throughout; MEDIUM/LOW items (enabledPlugins proto-guard, #6 path test, per-cause fix, #10 `===true`, `enabledMap` array-guard, stale JSDoc) ALL adopted · #10 golden = real `plugins-groundtruth` (13 installed+settings-enabled / 2 cached → exactly **11** cache-missing); a regression test pins record.enabled IGNORED. **465 tests / 0 fail**. · **P2.U5b-1** (plan U5b SPLIT — MCP probes #1/#2 now; hook probes #3/#5 → U5b-2; **DECISION RESOLVED**: keep doctor PURE, push the passive-probe I/O into a discovery GATHER per [[feedback-pure-analysis-split]]) — `src/lib/resolve-command.mjs` [`resolveCommand(command,{env,platform,cwd})`→`{resolved,path}`; OS-loader-style PATH+Windows-PATHEXT search via `statSync` ONLY, **never spawns**; uses host `node:path` because statSync is host-bound — injected `platform` controls only PATHEXT-on/off + PATH delimiter, NOT path.win32/posix; `isFile` rejects directories; path-like (absolute or contains a sep) resolved vs cwd, else PATH-searched] · `src/discovery/probe-mcp.mjs` [`gatherMcpProbes({configDir,mcpServers,env,platform,cwd})`→`{mcpAuth,mcpResolution,diagnostics}`; reads `<configDir>/mcp-needs-auth-cache.json` (`{name:{timestamp}}`)→`McpAuthFact[]` (proto-safe `isSafeKey`, finite-ts guard, per-entry-malformed warn); PATH-resolves each stdio server command→`McpResolutionFact[]`; `discover-bad-root` on bad configDir but resolution still runs (mirrors discoverMcp); never throws] · `src/analysis/doctor/probe-checks.mjs` [PURE #1+#2 + frozen `PROBE_CHECKS`; **#1 `mcp-auth-stale`** age>90d→error / >30d→warn (STRICTLY greater; future-ts→none); reads injected `input.now` and **returns [] when now is absent so the doctor NEVER calls Date.now()** (purity preserved); **#2 `mcp-server-resolvable`** stdio `resolved===false`→warn (advisory — probe-time PATH may differ from launch env)] · index.mjs DoctorInput += `mcpAuth/mcpResolution/now`, spreads `PROBE_CHECKS` → CHECKS `[1,2,6,7,8,9,10,11]` (zero new I/O in analysis — checks judge probe facts) · new `test/fixtures/mcp-auth-cache/` golden (3 valid @ 10/45/120d vs pinned now=1.8e12 + no-timestamp + non-object + `__proto__`) · reviewer (code-reviewer) APPROVE 0 Blocker/0 High; **HIGH PATHEXT-lowercase** (uppercase `.EXE` PATHEXT vs a lowercase `tool.cmd` MISSES on a case-SENSITIVE FS — invisible on NTFS, would fail Linux CI) FIXED + MEDIUM `localeCompare`→plain `</>` comparator (locale-independent, matches mcp.mjs `cmp`) + MEDIUM `isFinite`→`Number.isFinite` adopted; 1 MEDIUM (pre-existing `mcp.mjs:105` server-name proto-safety) DEFERRED out-of-scope; 3 LOWs intentional · **DOGFOOD (real ~/.claude):** 7 MCP servers (6 stdio + `exa` http correctly excluded from resolution); auth cache 5 entries @ 20/20/20/3/5d ALL <30d → #1=0; all 6 stdio commands `resolved` (5×`npx` via PATH + pencil ABSOLUTE `.exe` via the path-like branch) → #2=0; full doctor **0 diagnostics across all 8 checks** — NO false positive, both resolution branches validated on real data · `doctor` CLI wiring still P2.U11. **505 tests / 0 fail** (+16 resolve-command +7 probe-mcp +17 doctor-probe-checks). · **P2.U5b-2** (plan U5b SPLIT pt2 — hook probes #3/#5; **U5b now COMPLETE**) — pure/IO seam: `src/lib/hook-command.mjs` [PURE parser, no I/O: `tokenizeCommand` quote-aware split; `expandVars` `${VAR}`/`$VAR`/`%VAR%`/leading-`~` with **HOME→USERPROFILE fallback** + `fullyExpanded` flag, SINGLE combined-regex pass so a substituted value is never re-scanned; `classifyHookCommand`→`{kind:'file'|'external',target,fullyExpanded}|null` — interpreter-aware (node/python/pwsh/bash/deno/bun/ruby/perl/tsx/ts-node; **npx EXCLUDED** = its arg is a package), eval-flags (`-e/-m/-c/-p/--eval/--command/--print/--module`)→null so `node -e "code"`/`python -m mod` aren't mis-read as files] · `src/discovery/probe-hooks.mjs` [I/O: `gatherHookProbes({hooks,env,platform,cwd})`→`{hookFacts,diagnostics}`; walks merged `effective.hooks` (event→matcher-groups→`{type:'command',command}`), proto-safe event keys, classifies + resolves via `resolveCommand`; **file kind resolved to ABSOLUTE first** (bare filename → cwd, not PATH-searched); status `found`/`missing`/**`indeterminate`** (when `!fullyExpanded`, e.g. runtime-only `$CLAUDE_PROJECT_DIR` — must NOT flag); never throws] · `probe-checks.mjs` += **#3 `hook-file-exists`** (file+`missing`→**error**; indeterminate/found skipped) + **#5 `hook-external-command`** (external+`missing`→**warn**, PATH-context caveat) → `PROBE_CHECKS`/CHECKS now `[1,2,3,5,6,7,8,9,10,11]` (#4 = active, reserved for U7) · index.mjs DoctorInput += `hookFacts` typedef · new `test/doctor-hook-checks.test.mjs`; updated 3 registry-order assertions (doctor.test.mjs `[[1,0],[2,0],[3,0],[5,0],…]`, doctor-probe-checks.test.mjs id-list + `CHECKS.length` 8→10) · reviewer (code-reviewer) APPROVE 0 Blocker/0 High; **MEDIUM expandVars double-substitution** (3 sequential `.replace` passes re-scanned substituted text → cascade + wrong `fullyExpanded:true`) FIXED via single combined `VAR_RE` pass + regression test; 3 LOWs (findScriptArg value-flag NOTE + header refresh adopted; diagnostics:[] dead-field = no action) · **DOGFOOD (real ~/.claude):** 6 hook events → 8 facts (7 `node "$HOME/.claude/hooks/*.mjs"` → file, $HOME→USERPROFILE expanded, ALL `found`; `any-buddy apply --silent` → external `found`); the 9th `node $HOME/.claude/hud/omc-hud.mjs` correctly EXCLUDED (it is statusLine, not a hook); full doctor **0 diagnostics across all 10 checks** — NO false positive · `doctor` CLI wiring still P2.U11. **579 tests / 0 fail** (+50 hook-command incl. 1 double-sub regression, +11 probe-hooks, +13 doctor-hook-checks). · **P2.U6a** (plan U6 SPLIT pt1 — the 3 PURE judgment checks; the 9 I/O checks → U6b) doctor **#12 `orphan-files`** [judges `input.orphans` (OrphanRecord[] from analyzeOrphans∘detectOrphans) → one INFO per orphan, message `${category} orphan: ${path} — ${reason}`, sorted by message; skips non-object/empty-path records; category hard/soft/unknown], **#22 `claude-config-schema-version`** [escalates the discovery FACT `plugin-schema-version-unknown` (from `input.pluginDiagnostics`, emitted by plugins.mjs on an unknown installed_plugins.json schema version) → WARN under the canonical code; mirrors #6's escalate-a-discovery-fact pattern; a pinned comment keeps the filter code string in sync with plugins.mjs], **#23 `permissions-overbroad`** [flags `*`-containing entries in the merged `input.permissions.allow` → WARN, deduped + sorted; ONLY `allow` judged (broad deny is safe, broad ask only prompts)] · new `src/analysis/doctor/config-checks.mjs` exports frozen `CONFIG_CHECKS` (mirrors PROBE_CHECKS) spread into index.mjs CHECKS → registry `[1,2,3,5,6,7,8,9,10,11,12,22,23]` (length 13); DoctorInput += `orphans`/`pluginDiagnostics`/`permissions` — ZERO new discovery I/O (all 3 facts already gathered by orphan-detector/plugins/settings-merge), doctor stays pure · reviewer (code-reviewer) APPROVE 0 Blocker/0 High; adopted MEDIUM (#12 empty-string-path test) + 2 LOW (#22 code-pin comment, #23 fix interpolates the offending entry); DEFERRED MEDIUM strOr dup (index.mjs + config-checks.mjs both define it — extract to a shared doctor/util.mjs in U6b per rule-of-three; a reverse import would cycle) · **DOGFOOD (real ~/.claude):** #12=**16 info** (ALL genuine orphans, output matches the `orphans` cmd — NOT a #12 bug; but reveals orphan-detector `KNOWN_TOP_FILES` is too conservative: real CC runtime files history.jsonl/mcp-needs-auth-cache.json/.last-cleanup/bash-commands.log/cost-tracker.log/security_warnings_state_*.json + CLAUDE.md.backup.* + OMC's .omc-*.json/homunculus/metrics/session-data/teams all flagged → FOLLOW-UP: expand KNOWN_TOP_FILES; overlaps U6b #13 backup-bloat which owns CLAUDE.md.backup.*); #22=**0** (real installed_plugins.json is schema v2 = known → no fact → no finding; validates the no-fact→no-finding escalation path); #23=**14 warn** (ALL genuine wildcards — Edit(*)/Write(*)/NotebookEdit(*)/mcp__*/WebFetch(domain:*)/Skill(…:*)/Bash(gh api:*|git clone:*|curl:*|gh repo:*|npx claude-mem:*|cp …*) — no false +/−; non-wildcards Bash/WebSearch/mcp__pencil/Bash(npm --version)/quoted mkdir paths correctly skipped; per-spec, though scoped wildcards like Bash(gh api:*) are arguably benign → possible future severity-ranking refinement) · 615 tests / 0 fail; selftest --all exit 0. `doctor` CLI wiring still P2.U11. · **P2.U6b (plan U6 SPLIT pt2 — the 9 I/O passive checks; U6 now COMPLETE)** built in 3 sub-units (user-chosen granularity: split by I/O class), all honoring the pure-vs-IO fork ([[feedback-pure-analysis-split]]) — new I/O in discovery probes, doctor stays pure · **C0 chore**: extracted `strOr`/`numOr` → `src/analysis/doctor/util.mjs` (deferred U6a MEDIUM; index.mjs + config-checks.mjs import it) · **U6b-1** `src/discovery/probe-fs.mjs` [`gatherFsProbes({configDir,mgrStateDir,rulesDocPath})`→`{fsFacts,diagnostics}`; never-throws (safeReaddir/safeStatMtime), `discover-bad-root`+emptyFacts on bad configDir; FsFacts = claudeMdBackups{count,files}/snapshots[{path,mtimeMs}]/probeResidue[]/applyLeftovers[]/configRulesDoc] + `src/analysis/doctor/fs-checks.mjs` [**#13 claude-md-backup-bloat** >3→info, **#14 snapshot-retention** >90d→info, **#20 probe-residue** leftover→warn, **#21 apply-leftover-files** leftover→warn, **#25 config-rules-stale** >90d→info; #14/#25 read injected `input.now` & emit nothing when absent (purity, like #1); strict >90d boundary, future-mtime not flagged] → FS_CHECKS spread into CHECKS; reviewer APPROVE 0 Blocker/0 High, adopted MEDIUM dead-`!Number.isFinite` removal (#14/#25) · **U6b-2** probe-fs += `diskUsage{path,bytes}` via `safeDirSize` (recursive, **symlink-safe never-follows**, depth≥64 guard, finite-size guard) + new `src/discovery/probe-statusline.mjs` [`gatherStatuslineProbe` mirrors probe-hooks: classifyHookCommand+resolveCommand → found/missing/**indeterminate**] · **#16 disk-budget** (.mgr-state >5 GiB→warn — **target=.mgr-state per user decision**, forward-looking; in fs-checks) + **#18 statusline-resolvable** (status missing→warn; in probe-checks.mjs as a resolution sibling) · reviewer APPROVE, adopted depth≥64 + finite-guard LOWs · **U6b-3 (#17+#24, Windows-specific, synthetic tests per user choice)** `src/discovery/probe-access.mjs` [**#17** `gatherLockProbe` sync, read-only shared-open of settings.json → free/locked/absent/indeterminate, injectable `openFn`, detects only EXCLUSIVE locks (documented); **#24** pure `parseIcaclsAcl` (broad = Everyone/Users/Authenticated Users via last-`\`-token) + async `gatherAclProbe` — **FIRST `safeSpawn` consumer**, win32-only, never-rejects (unsupported/bad-root/absent/indeterminate/broad/owner-only), injectable `runIcacls`+`statFn` seams; icacls is READ-ONLY → passive] + `src/analysis/doctor/access-checks.mjs` [**#17 windows-file-locks** locked→warn; **#24 insecure-permissions** broad→warn naming principals] → ACCESS_CHECKS [17,24] · reviewer APPROVE 0 Blocker/0 High both sub-units; adopted MEDIUM `statFn` seam+test (non-ENOENT→indeterminate) + MEDIUM icacls-`/`-flag-blocked-by-positionalPattern doc + LOW parser multi-word-principal false-positive note (`Remote Desktop Users`→`Users`, accepted) + LOW `%USERNAME%` cmd-shell note · **REGISTRY now `[1,2,3,5,18,6,7,8,9,10,11,12,22,23,13,14,16,20,21,25,17,24]` length 22 = ALL 22 PASSIVE checks registered** (the remaining 3 of the plan's 25 — #4 hook-node-syntax, #15 claude-cli-resolvable, #19 loader-probe — are ACTIVE → P2.U7) · **DOGFOOD (real ~/.claude, consolidated all 9 U6b checks via ad-hoc gather):** backups 3 (boundary→no #13), snapshots/residue/leftovers 0, `.mgr-state` absent (diskUsage 0→no #16, acl 'absent'→no #24), settings.json lock 'free'→no #17, statusLine `omc-hud.mjs` 'found'→no #18, config-rules doc fresh→no #25 → **full doctor 0 diagnostics across all 22 passive checks, no false positives** · per-file commits + pushed; **763 tests / 0 fail**, lint clean, selftest --all exit 0. **U6 COMPLETE — passive doctor done.** ▷ **PHASE 2 ACTIVE PROBES (plan U7 SPLIT into U7a/b/c per user choice):** **P2.U7a** — the first ACTIVE check, doctor **#4 `hook-node-syntax`** [`node --check` per Node hook script; `syntax-error`→ERROR, `ok`/`indeterminate` silent; ACTIVE tier so it runs ONLY under `--active-probes`] · honors the pure-vs-IO fork ([[feedback-pure-analysis-split]]) — the spawn lives in discovery, the doctor stays pure · new `src/discovery/probe-hook-syntax.mjs` [`gatherHookSyntaxProbes({hookFacts,cwd,runNodeCheck})`→`{hookSyntax,diagnostics}`; CONSUMES the passive `hookFacts` so there is NO second tree-walk — only `kind:'file'`+`status:'found'`+`.mjs/.cjs/.js` targets are checked (missing files are #3's job; indeterminate skipped); **INVARIANT UPHELD: never executes the hook command STRING** — only `node --check <FILE_PATH>` (parse-only) via `safeSpawn` (`process.execPath` abs exe, `allowedFlags:['--check']`, `positionalPattern`=abs node-path, `maxArgs:2` → injection-proof; reviewer empirically fuzzed `-e`/`;rm`/extra-args, all rejected); err.code NUMBER→node-ran / STRING→indeterminate; **TOCTOU-hardened: a numeric non-zero exit WITHOUT a `SyntaxError:` in stderr (file deleted after the passive probe → 'Cannot find module') demotes to indeterminate**, never a false syntax error] · new `src/analysis/doctor/active-checks.mjs` [`ACTIVE_CHECKS` frozen array; #4 pure judge `probeLevel:'active'`; spread LAST in index.mjs CHECKS → registry ends `...,17,24,4`, active checks grouped at the end] · DoctorInput += `hookSyntax` · the `test/integration/doctor-no-hook-execution.test.mjs` DoD gate PROVES non-execution (a valid side-effect script that would write a sentinel IF run → asserted absent) + a TOCTOU case · reviewer (code-reviewer, opus) APPROVE 0 Blocker/0 High; 1 MEDIUM (TOCTOU mis-categorization) adopted + pinned by a real-spawn test, 3 LOW (tmpdir-cwd consistent-with-probe-access / `.mjs` bare-dotfile harmless / loose injected-runner JSDoc intentional) noted · the passive-default invariant test still PROVES via a SPY that #4's run() is never invoked without opt-in · **DOGFOOD (real ~/.claude):** the 7 node hook scripts ($HOME-expanded abs paths, e.g. keyword-detector/session-start/pre+post-tool-use/persistent-mode/code-simplifier) ALL `node --check` `ok` → #4 **0 findings**, no false positive (the 8th hook `any-buddy` is external → not node-checked) · per-file commits + pushed; **806 tests / 0 fail** (+43: probe-hook-syntax + doctor-active-checks + integration), lint clean, selftest --all exit 0. **U7a COMPLETE.** · **P2.U7b** — the second ACTIVE check, doctor **#15 `claude-cli-resolvable`** [verify the `claude` CLI is present + responsive] · new `src/discovery/probe-cli.mjs` [`gatherCliProbe({env,platform,cwd,resolveFn,runVersion})`→`{cli:CliFact,diagnostics}`; **RESOLUTION-PRIMARY** — resolve `claude` on PATH via `resolveCommand` (statSync, never spawns), then spawn `claude --version` via `safeSpawn` ONLY for a NATIVE exe (`isSpawnable`: win32→`.exe`/`.com` only, POSIX→any) so the Windows npm SHIM (extensionless/`.cmd`/`.ps1`, unrunnable under execFile w/o a shell since the Node CVE-2024-27980 fix) reports `status:'resolved'` — NEVER a false `'unresponsive'`; CliFact status `ok`/`resolved`/`unresolved`/`unresponsive`/`indeterminate`; **resolver-throw→`indeterminate`** (silent, not a false `unresolved` WARN); `extractVersion` semver parse is INPUT-CAPPED to 4 KiB to bound O(n²) regex backtracking on untrusted process stdout; never throws] · `active-checks.mjs` += #15 pure judge [WARN on `unresolved`+`unresponsive`, silent on `ok`/`resolved`/`indeterminate`] appended to ACTIVE_CHECKS → registry ends `...,24,4,15` (length 24) · DoctorInput += `cli` · reviewer (code-reviewer, opus) APPROVE 0 Blocker/0 High; 1 MEDIUM (extractVersion ReDoS-class) + 1 LOW (resolver-throw→indeterminate, the design question I posed) ADOPTED + pinned by tests, 2 LOW (one-code-per-check convention / version over-capture) noted-only · **DOGFOOD (real claude):** `claude --version`=`2.1.146`, but `resolveCommand('claude')`→the extensionless npm shim → `isSpawnable`(win32) false → `status:'resolved'` (no spawn) → #15 **0 findings**, no false positive — exactly the Windows-shim path the design protects · per-file commits + pushed; **853 tests / 0 fail** (+47), lint clean, selftest --all exit 0. **U7b COMPLETE.** · **P2.U7c-1** (U7c SPLIT pt1 — the write-gate change, isolated for a focused security review) — extended `assertWritable` (paths.mjs, the single governed-write gate) with a new tightly-scoped **`'probe'`** context: allows ONLY `<claudeDir>/agents/__mgr-probe-<uuid>.md` (regex `^__mgr-probe-[0-9a-f-]+\.md$`, DIRECTLY in agents/ via `dirname(canonical(target))===canonical(agentsDir)`), throws `write-probe-only` otherwise; placed AFTER the stateDir/outside-config/forbidden-subtree checks so probe context can't escape or reach marketplaces/projects; `canonical()` still denies a symlinked agents/ (→`write-outside-target`); apply/rollback byte-for-byte unchanged · boundary.mjs `buildAllowlistCases` += 3 probe cases (allow probe-name / deny non-probe-name / deny apply-on-probe-name) run against the REAL gate by `selftest --boundary` · reviewer (code-reviewer, opus, SECURITY pass) APPROVE **0 Blocker/0 High/0 Medium** — empirically defeated `agents/../` traversal, symlink-escape, NTFS ADS, 8.3 short-name, trailing-dot/space, multiline; 2 LOW (stale `@param` doc + a "fake is intentionally string-only" test note) adopted · **861 tests / 0 fail** (+8 probe-context), lint/invariants/boundary green. Gate ready for U7c-2's loader-probe.
- ▶ **NEXT: P2.U7c-2 (FINAL active probe — completes U7 + all 25 doctor checks)** — #19 `loader-probe` (active). **MECHANISM (user-chosen this session — option A, NO billable claude API call):** `gatherLoaderProbe` writes ONE `__mgr-probe-<uuid>.md` into the REAL `<configDir>/agents/` **via `assertWritable(target,'probe')`** (the gate landed in U7c-1), then self-checks via claude-mgr's own `discoverComponents(configDir)` that the probe agent is detected (end-to-end discovery check against the live dir), **cleans up in `finally`** (unlink + verify; cleanup-failure → residue, also caught later by the passive #20), and sets `confidence` from `loaderConfidence(ccVersion)` (real `claude --version`=`2.1.146` → 2.1.x verified; version from #15's CliFact or a fresh resolve). WHY no `claude -p`: there is NO cheap/side-effect-free claude introspection — only `claude -p` drives the loader, and that's a billable API call (rejected for a health check). · #19 doctor judge (append to `ACTIVE_CHECKS` → registry ends `...,24,4,15,19`): WARN on cleanup-failure (residue left) and on wrote-but-not-observed (discovery/path mismatch); confidence carried in the fact for load-order to cite. INVARIANT (plan): active probes NEVER invoke hook command strings. CAUTION: writes to the live harness — concurrency [[concurrent-sessions-shared-tree]], guarantee cleanup on throw/timeout. **P2.U7a+U7b+U7c-1 DONE (861 tests). After U7c-2: all 25 checks done → doctor CLI wiring is P2.U11.**
- ⏳ Carried follow-ups (non-blocking): (1) expand orphan-detector `KNOWN_TOP_FILES` to cover real CC runtime files (history.jsonl/mcp-needs-auth-cache.json/.last-cleanup/logs/security_warnings_state_* + CLAUDE.md.backup.* + OMC .omc-*.json/homunculus/metrics/session-data/teams) — reduces #12 noise + de-dups with #13 backup ownership (no real overlap today: backups=3 so #13 silent); (2) `safe-spawn` flag gate only rejects `-`-prefixed tokens — a future Windows-tool consumer passing `/flag` would slip the flag gate (icacls is safe only because `/`-tokens fail its positionalPattern); harden before more Windows spawners arrive. `doctor` CLI wiring remains **P2.U11**.
- ✅ Resolved follow-up: **M2** — DONE in U15 (`cli/resolve-config.mjs`). The live-config-dir branch try/catches the async paths.mjs import; a missing `~/.claude/hooks/lib` now surfaces a `missing-hooks-lib` warn `Diagnostic` + a direct `CLAUDE_CONFIG_DIR`/`~/.claude` fallback, so read-only commands run even when the hooks lib is absent (no raw loader error).

## Per-Unit Definition of Done (every work unit, in order)
1. **Regression** — full `node --test` green, no regressions.
2. **Code review** — a SEPARATE `code-reviewer` pass (never self-approve); fix Blocker/High before commit.
3. **Boundary** — edge cases covered (use the `test/fixtures/` corpus: broken / unicode-paths / long-paths / case-insensitive / …).
4. **Acceptance** — falsifiable criterion (golden-file oracle; "matches golden", never "exit 0").
5. **Git** — commit on green; **one file = one commit** (data fixtures may commit per-category); push.

## Environment gotchas (verified on this machine)
- Node **v24.14.0**; runner = `node --test` (zero deps). Run from the repo root.
- The **PowerShell tool is unreliable here** → use the **Bash tool with POSIX paths** (`/c/Dev/Projects/claude-mgr`).
- Windows **Developer Mode is ON** → the dogfood symlink uses `cmd /c mklink /D` (PowerShell `New-Item -SymbolicLink` fails).
- **Subagents may TIME OUT on long tasks here — but do NOT disable/abandon delegation.** The DoD's SEPARATE `code-reviewer` and `executor` passes MUST stay delegated (never self-author + self-approve — that gate is the whole point). Mitigate timeouts by scoping each delegated task SMALL (one sub-unit at a time) and **resuming/retrying** a timed-out agent; fall back to a direct Edit ONLY for a tiny fix a reviewer has already prescribed.
- The tool resolves the governed `~/.claude/` and `~/.claude/hooks/lib` via the home dir — **independent of cwd**, so develop here, not inside `~/.claude/`.
- GitHub remote: `exampleuser-jpg/claude-mgr` (PRIVATE), `main` → `origin/main`.
