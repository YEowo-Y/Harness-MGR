{
  "name": "claude-mgr",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test",
    "coverage": "c8 --reporter=text node --test",
    "coverage:json": "c8 --reporter=json-summary --reporter=text node --test"
  },
  "devDependencies": {
    "c8": "^11.0.0"
  }
}
